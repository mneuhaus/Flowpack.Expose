prototype(TYPO3.Expose:NodeTree) < prototype(TYPO3.TypoScript:FluidRenderer) {
	templatePath = 'resource://TYPO3.Expose/Private/Elements/RecordList/NodeTree.html'
	siteNode = ${siteNode}
	selectedFolderNode = ${selectedFolderNode}
}