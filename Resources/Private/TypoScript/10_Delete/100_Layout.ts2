# TODO: rename to "RemoveController" to be consistent
TYPO3.Expose.DeleteController.index = TYPO3.Expose:RemovalScreen

prototype(TYPO3.Expose:RemovalScreen) < prototype(TYPO3.TypoScript:FluidRenderer) {
	templatePath = 'resource://TYPO3.Expose/Private/Elements/Delete/Index.html'
	objects = ${objects}
	type = ${className}
	controllerCallback = 'delete'
}