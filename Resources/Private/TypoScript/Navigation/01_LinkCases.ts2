prototype(TYPO3.Admin:Navigation.Link) < prototype(TYPO3.TypoScript:Case)
#prototype(TYPO3.Admin:Navigation.Link) {
#	skipNonExistingTypes = ${true}
#}

# Catch-all condition for the default case
prototype(TYPO3.Admin:Navigation.Link).matchers.999999999999 {
	condition = ${true}
	type = 'TYPO3.Admin:Navigation.EntityOverviewLink'
}

prototype(TYPO3.Admin:Navigation.Link).matchers.10000 {
	condition = ${q(navigationLinkType).notEmpty()}
	type = ${navigationLinkType}
}


prototype(TYPO3.Admin:Navigation.DefaultLink) < prototype(TYPO3.TypoScript:FluidRenderer)
prototype(TYPO3.Admin:Navigation.DefaultLink) {
	item = ${item}
	options = ${options}
}

prototype(TYPO3.Admin:Navigation.EntityOverviewLink) < prototype(TYPO3.Admin:Navigation.DefaultLink)
prototype(TYPO3.Admin:Navigation.EntityOverviewLink) {
	templatePath = 'resource://TYPO3.Admin/Private/Elements/Navigation/EntityOverviewLink.html'
}

prototype(TYPO3.Admin:Navigation.EntityFilterLink) < prototype(TYPO3.Admin:Navigation.DefaultLink)
prototype(TYPO3.Admin:Navigation.EntityFilterLink) {
	templatePath = 'resource://TYPO3.Admin/Private/Elements/Navigation/EntityFilterLink.html'
}

prototype(TYPO3.Admin:Navigation.EntityActionLink) < prototype(TYPO3.Admin:Navigation.DefaultLink)
prototype(TYPO3.Admin:Navigation.EntityActionLink) {
	templatePath = 'resource://TYPO3.Admin/Private/Elements/Navigation/EntityActionLink.html'
}